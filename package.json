{
  "private": true,
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "@headspace/web-styles": "^4.3.6",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
